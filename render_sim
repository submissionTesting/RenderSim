#!/usr/bin/env bash
# Forwarder script for RenderSim CLI
# Usage: ./render_sim <command> [args]

set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/CLI/main.py" "$@" 